{
  "name": "@json-feed-types/root",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @json-feed-types/common && npm run build -w @json-feed-types/1_1",
    "dev": "npm run dev --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present",
    "format": "prettier . -w"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^11.1.6",
    "@types/node": "^20.14.2",
    "prettier": "^2.4.1",
    "rollup": "^4.18.0",
    "tslib": "^2.6.2",
    "typescript": "^5.4.5"
  }
}
